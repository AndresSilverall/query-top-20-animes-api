[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = "*"
graphene-django = "*"

[dev-packages]

[requires]
python_version = "3.8"

[scripts]
run = "python manage.py run server 8000"
make = "python manage.py makemigrations"
migrate = "python manage.py migrate"
